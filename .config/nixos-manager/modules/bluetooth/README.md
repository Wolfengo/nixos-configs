# Bluetooth Module

Включает поддержку Bluetooth в NixOS.

- `enable = true` — включает сервис.
- `powerOnBoot = true` — включает адаптер при старте.

Полезные пакеты:
- `bluez` — стек Bluetooth.
- `blueman` — GUI для управления.
